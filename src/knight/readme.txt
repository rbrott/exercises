---Knight's Tour---

This program does a depth first search on all of the possible combinations of the Knight's Tour.
The algorithm is deterministic and slow so the output may not be ready for quite a few seconds.
After computation, the solution is then drawn to the screen.
